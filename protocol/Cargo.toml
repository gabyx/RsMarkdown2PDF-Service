[package]
name = "protocol"
version = "0.1.0"
edition = "2021"

[dependencies.serde]
version = "1.0"
features = ['derive']


[dependencies.uuid]
version = "1.5.0"
features = [
    "v4",
    "serde"
]
