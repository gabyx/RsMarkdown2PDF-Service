kind: Service
apiVersion: v1
metadata:
  namespace: default
  name: converter
  labels:
    app: converter
    type: LoadBalancer
spec:
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 8001
      targetPort: 8001
  selector:
    app: converter
